@model PagedViewModel<PersonalInfo>

@{
    ViewData["Title"] = "Index";
}

<table class="table table-dark " style="width:1000px">
    <thead>
        <tr>
            <th scope="col">CompanyName</th>
            <th scope="col">Occuption</th>
            <th scope="col">Location</th>
            <th scope="col">LinkForDiplom</th>

            <th scope="col" class="btn btn-success" style="height:45px; margin-top:5px; margin-left:10px;color:white; width:250px" float="right"><a style="color: white;" asp-action="Create">Create New</a></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Items)
        {
        <tr>

            <th scope="row">@item.Location</th>
            <td>@item.Degree</td>
            <td>@item.PhoneNumber</td>
            <td>@item.Email</td>

            <td>
                <a class="btn btn-primary" style="width:70px" asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>

        </tr>

        }

    </tbody>
</table>
